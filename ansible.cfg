[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
stdout_callback = yaml
# Un parc de vingt commutateurs : cinq en parallèle suffisent et restent lisibles.
forks = 5

[persistent_connection]
command_timeout = 60
connect_timeout = 60
